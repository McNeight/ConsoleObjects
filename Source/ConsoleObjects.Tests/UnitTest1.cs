using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace ConsoleObjects.Tests
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestMethod1()
        {
            //var rootBuffer = new Buffer();
            //rootBuffer.Size = Size.FromValues(10, 10);

            //var childBuffer = new Buffer();
            //childBuffer.Size = Size.FromValues(10, 10);
            //childBuffer.OffsetPosition = Position.FromValues(2, 0);

            //var layer = new Layer(rootBuffer);
            //layer.Buffers.Add(childBuffer);

            //BorderPrinter.Print2(layer);
            //layer.Init();
        }
    }
}
